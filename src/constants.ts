export const AGENT_TYPE_ENV_VARS = ["SANEPI_AGENT_TYPE", "PI_AGENT_TYPE"] as const;
export const CONFIG_DIR_NAMES = [".pi", ".senpi"] as const;
export const AGENT_SUBDIRECTORIES = ["agent", "agents"] as const;
